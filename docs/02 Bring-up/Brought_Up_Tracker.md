# Brought-Up Tracker

Use this as the live progress tracker for HW5 hardware and firmware bring-up.

---

## Metadata

- Last updated: `2026-05-30`
- Board revision: `TBD`
- Firmware commit: `TBD`
- Maintainer: `TBD`

---

## Bring-Up Phase Status

| Phase | Status | Primary Runbooks | Notes |
|---|---|---|---|
| 0 - Arrival, power, and clock stability | In progress | [[HW5_Arrival_Phase0_Checklist]], [[ADP5360_Power_Bring-up_Runbook]], [[HW5_Clock_Tree_Contract]], [[FW0_Phased_CubeMX_Bring-up_Plan]], [[Bounded_Build_Flash_Debug_Runbook]] | `fw0` Phase 0A baseline built/flashed/attached; Phase 0B ADP5360 I2C probe ACK/readback succeeded; PMIC rail status and user DMM readings confirm `1.80 V` and `3.30 V` rails; keep phase open for measured current trace, reset-cause, and clock evidence |
| 1 - Display validation | In progress | [[LS013B7DH05_Display_Bring-up_Runbook]] | `fw0` display-minimum CubeMX baseline generated and no-display safe-state probe passed; user measured 1 Hz EXTCOMIN on `PC13`; TXU0104 OE polarity corrected to active-high; connected display now visibly full-screen inverts using blocking SPI3 hardware-NSS chunks and LPDMA `48+48+48+24` row-window chunks at 1 Mbit/s; static pattern hold passed after writes stopped; LPDMA partial row-window update passed; minimal renderer dirty-row path passed; uniform fill polarity proven as `0x00` black and `0xFF` white; first single-transfer LPDMA full-frame trial failed; current measured LPDMA row-window boundary is 51 rows / 1022 bytes passing and 52 rows / 1042 bytes failing |
| 2 - Storage validation | In progress | [[AT25SL128A_External_Flash_Bring-up_Runbook]] | `fw0` OCTOSPI1 conservative baseline generated and built; JEDEC/status probe passed at 64 MHz kernel / prescaler 8; destructive scratch-sector erase/program/polling-readback/cleanup-erase passed on `0x00FFF000`; deep-power-down entry/release, JEDEC revalidation, and post-wake scratch blank readback passed; GPDMA page program and readback of programmed scratch data passed after enabling the required OCTOSPI1 IRQ completion path; filesystem/storage-owner integration remains pending |
| 3 - Audio validation | In progress | [[Audio_Output_Bring-up_Runbook]] | Phase 3A BBB fixed-tone, rising sweep, and short melody/gap pattern paths passed on `PB2` / `LPTIM1_CH1` using MSIK 4 MHz; user heard the beep, sweep, and melody; SAI1 speaker path produced audible generated `1 kHz` PCM tones through GPDMA TX after fixing speaker connection; PPK2 ladder indicates sine amplitude `3000` is a usable bring-up cue around `15 mA`, while quieter sine steps and short square cue were around `10 mA`; mixer/asset/concurrent-output validation remains pending |
| 4 - Input and sensors | In progress | [[Button_Input_Bring-up_Runbook]], [[Rotary_Encoder_Bring-up_Runbook]], [[TEMT6000_Light_Sensor_Bring-up_Runbook]], [[LIS2DUX12_IMU_Bring-up_Runbook]], [[TMAG3001_Joystick_Bring-up_Runbook]] | Normal button GPIO/EXTI bring-up passed for Start, A, B, L, and R using raw `fw0` probe; light sensor polled ADC path responds to ambient versus direct light with inverse ADC polarity; LIS2DUX12 read-only identity/status probe passed at I2C address `0x18`; BOOT0, debounce/hold/repeat/chord policy, encoder, joystick, IMU step counter/INT1/motion functions, light calibration bands, and light streaming policy remain pending |
| 5 - RTOS owner integration | Not started | [[RTOS_Ownership_and_Queue_Topology]], [[Subsystem_State_Machines]] | owner threads and queues match contracts |
| 6 - Sleep and wake validation | Not started | [[Sleep_Wake_Integration_Bring-up_Runbook]] | wake reason classification and measured current |
| 7 - Installer and transport mode | In progress | [[USB_MSC_Bring-up_and_Recovery_Runbook]], [[USB_Development_Mode_Contract]], [[NINA_B112_BLE_Bring-up_Runbook]] | USB MSC staging first; CDC developer personality is separate and mutually exclusive; preliminary NINA reset release and UART `AT` proof passed at `115200`; advertising, connection, pairing/bonding, suspend/resume, and comm-owner integration remain pending |
| 8 - Runtime host lifecycle | Not started | [[Runtime_Host_Contract]], [[Package_Contract]] | only after hardware owners are stable |
| 9 - Platform freeze checks | Not started | [[Platform_Freeze_Charter]], [[HW5_Hardware_Documentation_Readiness]] | authority docs and measured evidence agree |
| 10 - Digital twin parity | Blocked | [[Digital_Twin_Host_Runtime_Contract]], [[Validation_Plan]] | blocked until HW5 Platform hardware validation and Platform contract behavior are proven |

---

## Digital Twin Sequencing Rule

The digital twin is not a bring-up substitute.

Do not implement or validate the host digital twin as a source of hardware truth before HW5 Platform hardware validation is complete.

Digital twin work may begin only after measured Platform behavior is recorded for the hardware/backend contracts it mirrors.

Digital twin evidence can validate package, Engine, runtime lifecycle, state graph, input, save, cadence, and cross-mode contract behavior.

Digital twin evidence cannot mark hardware behavior known-good. Hardware behavior is known-good only when measured on HW5 hardware and linked from this tracker.

---

## Evidence Ledger

Evidence artifacts should follow [[Evidence_Artifact_Convention]].

Measured constants promoted by an evidence entry should update [[Pending_Measured_Constants_Register]].

For each evidence entry record:

- date/time
- evidence ID
- test case ID
- mode/runtime class
- result
- artifacts
- command line or orchestration CLI invocation
- notes
- trace profile and Tracealyzer artifact path, if a trace snapshot was used
- telemetry capture artifact path and schema version, if dashboard telemetry was used
- current/power capture artifact path, instrument setup, and sync strategy, if PPK2 or equivalent measurement was used
- active tuning overlay and instrumentation state, if applicable

Template row:

| Date | Test Case | Mode/Host | Result | Artifact | Notes |
|---|---|---|---|---|---|
| YYYY-MM-DD | T-XXX | SHELL | PASS | path/to/log | |
| 2026-05-28 | PH0A-FW0-GEN-BUILD | `fw0` / bounded build | PASS | `firmware/peepshow_hw5_fw0/__fw0_build_stdout_2.txt`; `firmware/peepshow_hw5_fw0/__fw0_build_stderr_2.txt` | Active bring-up target `firmware/peepshow_hw5_fw0/peepshow_hw5_fw0.ioc`; bounded Debug build produced `build/Debug/peepshow_hw5_fw0.elf`; final no-op build returned `ninja: no work to do`; RAM `1584 B`, ROM `8344 B`, SRAM4 `0`; no leftover build processes. |
| 2026-05-28 | PH0A-FW0-FLASH | `fw0` / STLINK-V3MINIE | PASS | `firmware/peepshow_hw5_fw0/__fw0_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_flash_stderr.txt` | Flashed `build/Debug/peepshow_hw5_fw0.elf` with `STM32_Programmer_CLI.exe -c port=SWD freq=4000 -w ... -v -rst`; verify and software reset reported; target voltage `1.80 V`; user reported PPK2/thermal/visual state looked good after reset, qualitative only, no numeric current trace recorded. |
| 2026-05-28 | PH0A-FW0-DEBUG-STATUS | `fw0` / bounded GDB attach | PASS | `firmware/peepshow_hw5_fw0/__fw0_gdb_stdout_2.txt`; `firmware/peepshow_hw5_fw0/__fw0_gdbserver_stdout_2.txt` | Read-only attach, register read, detach; no `continue`, stepping, reset, or resume. PC `0x08001fb8 <Reset_Handler>`, SP `0x200c0000`, XPSR `0x01000000`; target voltage `1.81 V`; no breakpoints/watchpoints; no leftover GDB/ST-Link processes. |
| 2026-05-28 | PH0B-ADP5360-I2C-PROBE | `fw0` / I2C3 + bounded GDB readback | PASS | `firmware/peepshow_hw5_fw0/__fw0_phase0b_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0b_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0b_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0b_gdbserver_stdout.txt` | I2C3 on `PC0`/`PC1`; probe struct `g_ps_phase0b_pmic_probe` showed public 7-bit address `0x46` ACKs when passed to STM32 HAL as shifted `0x8C`; unshifted `0x46` failed as expected; registers `0x00..0x0F` read successfully: `10 08 81 7a 29 2b 07 2c 00 00 00 56 8f 71 b4 00`; flash verify/reset passed; target voltage `1.79-1.80 V`; no leftover Programmer/GDB/ST-Link processes. |
| 2026-05-28 | PH0B-ADP5360-RAIL-STATUS-SNAPSHOT | `fw0` / I2C3 + bounded GDB readback + DMM | PASS | `firmware/peepshow_hw5_fw0/__fw0_phase0b_rail_probe_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0b_rail_probe_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0b_rail_probe_gdb_stdout_3.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0b_rail_probe_gdbserver_stdout_3.txt` | Read-only ADP5360 snapshot expanded to registers `0x00..0x36` except read-to-clear interrupt flags `0x34/0x35`; flash verify/reset passed; target voltage `1.79-1.81 V`; `PGOOD_STATUS=0x07` reported `VOUT1OK`, `VOUT2OK`, and `BATOK`; buck target decoded `1800 mV`, buck-boost target decoded `3300 mV`; user measured rails by multimeter at `1.80 V` and `3.30 V`, matching PMIC decoded targets; `BAT_SOC=0`, `VBAT_READ_H/L=00 00`, so PMIC fuel/ADC battery voltage is not usable evidence yet; first GDB command attempt failed due path quoting before target attach, rerun succeeded; no leftover Programmer/GDB/ST-Link processes. |
| 2026-05-28 | PH0-RESET-CLOCK-SNAPSHOT | `fw0` / bounded flash + GDB readback | PASS_WITH_LIMITATION | `firmware/peepshow_hw5_fw0/__fw0_phase0_reset_clock_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0_reset_clock_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0_reset_clock_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase0_reset_clock_gdbserver_stdout.txt` | Added read-only `g_ps_phase0_reset_clock_probe`; bounded build passed with RAM `2120 B`, ROM `17344 B`, SRAM4 `0`; flash verify/reset passed, target voltage `1.81 V`; GDB readback showed `SystemCoreClock`, `SYSCLK`, `HCLK`, `PCLK1`, `PCLK2`, and `PCLK3` all `4000000 Hz`, matching intentional `fw0` Phase 0 4 MHz MSI bring-up baseline; reset flags showed `PINRST`, `BORRST`, and `SFTRST` set with watchdog/low-power flags clear, but flags were not cleared before a controlled reset-cause test, so this is accumulated reset evidence only; no breakpoints/watchpoints; no leftover Programmer/GDB/ST-Link processes. |
| 2026-05-28 | PH0B-ADP5360-DRIVER-INTEGRATION | `fw0` / imported driver + bounded flash/GDB | PASS | `firmware/peepshow_hw5_fw0/__fw0_adp5360_harness_build2_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_adp5360_driver_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_adp5360_driver_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_adp5360_driver_gdbserver_stdout.txt` | Added imported `Core/Inc/ADP5360.h` and `Core/Src/ADP5360.c` to `fw0`; `ADP5360_init()` was constrained to read-only identity/revision reads by removing its `ADP5360_power_init(&ADP_cfg)` call; bounded build passed with RAM `2120 B`, ROM `18328 B`, SRAM4 `0`; flash verify/reset passed, target voltage `1.81 V`; GDB readback showed `driver_init_status=0`, public address `0x46` / HAL shifted `0x8C`, same register snapshot as raw Phase 0B probe, `fault=0x00`, `PGOOD_STATUS=0x07`, buck target `1800 mV`, buck-boost target `3300 mV`; no breakpoints/watchpoints; no leftover Programmer/GDB/ST-Link processes. |
| 2026-05-28 | PH1-DISPLAY-NO-PANEL-BASELINE | `fw0` / display-minimum CubeMX + bounded flash/GDB | PASS_WITH_LIMITATION | `firmware/peepshow_hw5_fw0/__fw0_phase1_display_baseline_probe_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_baseline_probe_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_baseline_probe_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_baseline_probe_gdbserver_stdout.txt` | Display panel was not physically connected. `fw0` generated with SPI3 blocking baseline (`PC10` SCK, `PC12` MOSI, `PA15` hardware NSS active high, 1-line TX, LSB first, NSS pulse disabled, 4 MHz MSIK kernel / ~2 Mbit/s), RTC/LSE bypass `PC13` 1 Hz calibration output path, and `PD2` `VLT_LCD` push-pull output default high. Later datasheet check confirmed `VLT_LCD`/TXU0104 OE is active-high, so pin state `1` means translator enabled, not disabled. Bounded build passed with RAM `2352 B`, ROM `28328 B`, SRAM4 `0`; flash verify/reset passed, target voltage `1.79 V`; GDB readback showed ADP5360 still ACK/readback OK after I2C timing/clock change, `VLT_LCD` ODR/IDR `0x4` and pin state `1`, SPI3 kernel `4000000 Hz`, `spi3_nssp_disabled=1`, `spi3_lsb_first=1`, `spi3_nss_active_high=1`; no display transfer was attempted; no leftover Programmer/GDB/ST-Link processes. |
| 2026-05-28 | PH1-DISPLAY-EXTCOMIN-CONNECTOR | `fw0` / DMM at display connector | PASS | `firmware/peepshow_hw5_fw0/__fw0_phase1_display_baseline_probe_gdb_stdout.txt` | With display-minimum baseline firmware running, user measured the RTC 1 Hz EXTCOMIN/LCD_1HZ signal reaching the display connector by multimeter. This confirms the 1 Hz path through the enabled measurement setup at connector level; scope waveform quality, panel-side behavior with display physically connected, and image hold behavior remain pending. |
| 2026-05-28 | PH1-DISPLAY-CONNECTED-INVERT-ATTEMPT | `fw0` / connected panel + blocking SPI3 fill-frame loop | FAIL_ROOT_CAUSED | `firmware/peepshow_hw5_fw0/__fw0_phase1_display_invert_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_invert_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_invert_status_gdb_stdout_2.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_invert_status_gdbserver_stdout_2.txt` | User connected the display panel and reported no visible display response and nothing noteworthy on PPK2. `fw0` blocking invert test drove `VLT_LCD` low and alternated full-screen `0x00`/`0xFF` payloads once per second using SPI3 blocking transmit only, no DMA/renderer/SRAM4/LPBAM. Later TXU0104 datasheet check and user scope/DMM observations confirmed `VLT_LCD`/OE is active-high, so this firmware disabled the level translator during the attempted update. Bounded build passed with RAM `5760 B`, ROM `30304 B`, SRAM4 `0`; flash verify/reset passed, target voltage `1.80 V`; bounded GDB readback showed `g_ps_phase1_display_invert_probe.frame_counter=31`, `tx_ok_count=31`, `tx_error_count=0`, `last_tx_status=0`, `last_hal_error=0`, payload length `0x0d22` (`3362`) and `vlt_lcd_pin_state=0`. SPI/NSS/MOSI/SCK panel-side behavior with active-high OE remains pending. |
| 2026-05-28 | PH1-DISPLAY-CLEAR-COMMAND | `fw0` / imported LS013B7DH05 driver adapted for SPI3 hardware NSS | PASS_WITH_LIMITATION | `firmware/peepshow_hw5_fw0/__fw0_phase1_display_driver_hwnss_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_driver_hwnss_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_driver_hwnss_status_gdb_stdout_2.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_driver_hwnss_status_gdbserver_stdout_2.txt` | User reported the connected display cleared from static noise after flashing the hardware-NSS driver build. This is retained as observed panel evidence, but later TXU0104 polarity validation showed the follow-on loop drove `VLT_LCD` low, which disables panel-side signals. Bounded flash verify/reset passed at target voltage `1.80 V`; bounded GDB readback showed the driver loop executing with `frame_counter=43`, `tx_ok_count=43`, `tx_error_count=0`, `last_tx_status=0`, `last_hal_error=0`, and `VLT_LCD` pin state `0` after initialization. Full-frame visible pattern with `VLT_LCD` high/enabled, pixel polarity, row order, and static hold remain pending. |
| 2026-05-28 | PH1-DISPLAY-VLT-OE-POLARITY | `fw0` / TXU0104 datasheet check + user probing | PASS | `docs/07 Hardware/HW5_Power_Rails.md`; `docs/01 Platform/Rendering/Display_and_Rendering_Contract.md`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_vlt_active_high_build_stdout.txt` | User checked the TXU0104 datasheet and measured `PD2` low while `PC13` still toggled 1 Hz on the MCU side. Datasheet confirms OE is active-high: `VLT_LCD` high enables level translation and low places outputs high-Z. Firmware display loop was corrected to drive `VLT_LCD` high before `LCD_Init()`/`LCD_FlushAll()`. Relevant display hardware contracts/runbooks were corrected from active-low to active-high. Bounded build passed with RAM `8792 B`, ROM `33962 B`, SRAM4 `0`; flash and visible pattern retest remain pending. |
| 2026-05-29 | PH1-DISPLAY-ROW-WINDOW-DIAGNOSTIC | `fw0` / connected panel + blocking SPI3 row chunks | PASS | `firmware/peepshow_hw5_fw0/__fw0_phase1_display_row_window_altfill_build_stdout.txt` | User built/flashed the row-window diagnostic and observed visible 8-row windows moving through rows `1-8`, `9-16`, `17-24`, and `25-32`, with invisible intervals matching the programmed fill-polarity flip. This confirmed row addresses above the first 8 rows work when sent as bounded `LCD_FlushRows()` chunks and that the previous no-visible result was partly diagnostic polarity/contrast, not a dead panel. |
| 2026-05-29 | PH1-DISPLAY-CHUNKED-FULL-INVERT | `fw0` / connected panel + blocking SPI3 8-row chunks | PASS | `firmware/peepshow_hw5_fw0/__fw0_phase1_display_chunked_full_invert_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase1_display_chunked_full_invert_noop_stdout.txt` | User built/flashed the chunked full-screen proof and reported the full display now visibly inverts. Firmware path clears and then writes all `168` rows as 8-row `LCD_FlushRows()` chunks over SPI3 hardware NSS, alternating `0x00`/`0xFF` each pass. Bounded build passed with RAM `8792 B`, ROM `34242 B`, SRAM4 `0`; no-op build passed. This validates connected-panel full-screen visible update, active-high `VLT_LCD`, row addressing via chunked partial writes, and blocking SPI3 hardware-NSS signal path. The single contiguous `3362` byte `LCD_FlushAll()` transaction only affected top rows and remains pending debug before being used for normal full-frame/DMA/LPBAM paths. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-FULL-FLUSH-TRIAL | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | FAIL_ROOT_CAUSED | `firmware/peepshow_hw5_fw0/__fw0_lpdma_fullflush_sram4_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_fullflush_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_status_gdb_stdout_2.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_status_gdbserver_stdout_2.txt`; `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.map` | Firmware display loop alternated full-frame `LCD_FlushAll_DMA()` transfers and waited for `LCD_FlushDMA_IsDone()` with a `1000 ms` timeout. CubeMX-generated LPDMA1 channel 0 is linked to SPI3 TX, and the linker places `.sram4` at `0x28000000`; clean bounded build passed with RAM `5560 B`, ROM `37096 B`, SRAM4 `3368 B`, elapsed `2.557 s`, exit code `0`, no leftover build processes. Bounded flash programmed `peepshow_hw5_fw0.elf`, Programmer output reported `Download verified successfully`, `MCU Reset`, and `Software reset is performed`, elapsed `0.796 s`, no leftover Programmer processes. User observed only the top roughly `16` rows black and static. Bounded non-resume GDB readback showed `frame_counter=0x1e5`, `tx_ok_count=0`, `tx_error_count=0x1e5`, `last_tx_status=0x2` (`HAL_BUSY`), `hspi3.State=0x3` (`BUSY_TX`), DMA state `0x2` (`BUSY`), no SPI/DMA error code, requested payload `0x0d22`, SPI `CR2=0x0122`, and LPDMA `CBR1=0x0c00`; the remaining-count values sum to the requested payload size. The current single-transfer `3362` byte DMA strategy stalls mid-transfer. Next path should use protocol-valid row-window DMA transactions and scale the row-window size from the known-good 8-row case. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-8ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_8row_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_8row_flash_stdout.txt` | Firmware display loop alternated rows `1-8` using `LCD_FlushRows_DMA()` and a `1000 ms` completion timeout. Bounded build passed with elapsed `0.768 s`, exit code `0`, no leftover build processes. Bounded flash programmed the fw0 ELF with verify and reset markers present, elapsed `1.292 s`, no leftover Programmer processes. User observed the first chunk of rows visibly inverting. This validates the generated LPDMA1 CH0 SPI3 TX path, SRAM4 TX source placement, hardware NSS, and Sharp row-window stream for an 8-row DMA transaction. Larger row-window sizes remain pending. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-16ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_16row_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_16row_flash_stdout.txt` | Firmware display loop alternated rows `1-16` using `LCD_FlushRows_DMA()` and a `1000 ms` completion timeout. Bounded build passed with elapsed `0.662 s`, exit code `0`, no leftover build processes. Bounded flash programmed the fw0 ELF with verify and reset markers present, elapsed `1.194 s`, no leftover Programmer processes. User observed the 16-row window visibly inverting. Larger row-window sizes remain pending. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-32ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_32row_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_32row_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_32row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_32row_1mbit_flash_stdout.txt` | Firmware display loop alternated rows `1-32` using `LCD_FlushRows_DMA()` and a `1000 ms` completion timeout. At the previous slow SPI rate, bounded build passed with elapsed `0.696 s`, exit code `0`, and flash verify/reset passed with elapsed `1.178 s`; user observed the 32-row window visibly inverting with line-by-line draw visible. User then increased SPI3 to CubeMX-calculated `1000.0 KBits/s` (`SPI_BAUDRATEPRESCALER_4`). Faster 32-row bounded build passed with elapsed `0.896 s`, exit code `0`, no leftover build processes; flash verify/reset passed with elapsed `0.857 s`, no leftover Programmer processes. User observed the 32-row window inverting cleanly and fast. Larger row-window sizes remain pending. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-64ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | FAIL_OBSERVED | `firmware/peepshow_hw5_fw0/__fw0_lpdma_64row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_64row_1mbit_flash_stdout.txt` | Firmware display loop alternated rows `1-64` using `LCD_FlushRows_DMA()` at CubeMX-calculated `1000.0 KBits/s` and a `1000 ms` completion timeout. Bounded build passed with elapsed `0.651 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `0.875 s`, no leftover Programmer processes. User observed static first rows again rather than a clean 64-row inversion. The stable DMA row-window limit is therefore below 64 rows; 48-row test is the next narrowing step. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-48ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_48row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_48row_1mbit_flash_stdout.txt` | Firmware display loop alternated rows `1-48` using `LCD_FlushRows_DMA()` at CubeMX-calculated `1000.0 KBits/s` and a `1000 ms` completion timeout. Bounded build passed with elapsed `0.691 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.179 s`, no leftover Programmer processes. User observed the 48-row window visibly inverting. The failure boundary is now between 48 and 64 rows. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-56ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | FAIL_OBSERVED | `firmware/peepshow_hw5_fw0/__fw0_lpdma_56row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_56row_1mbit_flash_stdout.txt` | Firmware display loop alternated rows `1-56` using `LCD_FlushRows_DMA()` at CubeMX-calculated `1000.0 KBits/s` and a `1000 ms` completion timeout. Bounded build passed with elapsed `0.677 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `0.795 s`, no leftover Programmer processes. User observed a stall rather than clean inversion. The stable DMA row-window boundary is now between 48 and 56 rows. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-52ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | FAIL_OBSERVED | `firmware/peepshow_hw5_fw0/__fw0_lpdma_52row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_52row_1mbit_flash_stdout.txt` | Firmware display loop alternated rows `1-52` using `LCD_FlushRows_DMA()` at CubeMX-calculated `1000.0 KBits/s` and a `1000 ms` completion timeout. Bounded build passed with elapsed `0.669 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `0.812 s`, no leftover Programmer processes. User observed no rows inverting. The stable DMA row-window boundary is now between 48 and 52 rows. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-50ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_50row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_50row_1mbit_flash_stdout.txt` | Firmware display loop alternated rows `1-50` using `LCD_FlushRows_DMA()` at CubeMX-calculated `1000.0 KBits/s` and a `1000 ms` completion timeout. Bounded build passed with elapsed `0.712 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `0.841 s`, no leftover Programmer processes. User observed the 50-row window visibly inverting. The stable DMA row-window boundary is now between 50 and 52 rows. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-51ROW-WINDOW | `fw0` / generated LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_51row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_51row_1mbit_flash_stdout.txt` | Firmware display loop alternated rows `1-51` using `LCD_FlushRows_DMA()` at CubeMX-calculated `1000.0 KBits/s` and a `1000 ms` completion timeout. Bounded build passed with elapsed `1.628 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.239 s`, no leftover Programmer processes. User observed the 51-row window visibly inverting. Since the Sharp row-window stream is `20 * rows + 2` bytes, 51 rows is `1022` bytes and 52 rows is `1042` bytes; the observed boundary is consistent with a practical 1024-byte transaction limit in the current LPDMA/SPI path. |
| 2026-05-29 | PH1-DISPLAY-GPDMA-52ROW-SRAM4 | `fw0` / GPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | FAIL_OBSERVED | `firmware/peepshow_hw5_fw0/__fw0_gpdma_52row_1mbit_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_gpdma_52row_1mbit_flash_stdout.txt` | CubeMX added GPDMA1 channel 0 with `GPDMA1_REQUEST_SPI3_TX` while retaining LPDMA1 channel 0. Generated MSP links GPDMA first and LPDMA second, so firmware explicitly relinked `hspi3.hdmatx` to `handle_GPDMA1_Channel0` before `LCD_FlushRows_DMA()`. Firmware tested a 52-row Sharp row-window stream at `1000.0 KBits/s` while the driver's DMA staging buffer remained in `.sram4` at `0x28000000`. Bounded build passed with elapsed `1.409 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.164 s`, no leftover Programmer processes. User observed nothing on the display. This does not validate GPDMA; next comparison should place the TX staging buffer in normal RAM to separate GPDMA routing from SRAM4 source access. |
| 2026-05-29 | PH1-DISPLAY-GPDMA-52ROW-NORMAL-RAM | `fw0` / GPDMA1 CH0 SPI3 TX + normal-RAM TX buffer | FAIL_ROOT_CAUSED | `firmware/peepshow_hw5_fw0/__fw0_gpdma_52row_normalram_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_gpdma_52row_normalram_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_gpdma_status_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_gpdma_status_gdbserver_stdout.txt` | Firmware explicitly relinked `hspi3.hdmatx` to `handle_GPDMA1_Channel0` before `LCD_FlushRows_DMA()` and tested a 52-row Sharp row-window stream at `1000.0 KBits/s`. The driver's DMA staging buffer was moved out of `.sram4` into normal RAM for this comparison; build memory reported SRAM4 `0 B`. Bounded build passed with elapsed `0.696 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.122 s`, no leftover Programmer processes. User still observed a blank display. Bounded non-resume GDB readback showed `hspi3.hdmatx` points at `handle_GPDMA1_Channel0`, GPDMA request `0xb` (`GPDMA1_REQUEST_SPI3_TX`), `hspi3.State=0x3` (`BUSY_TX`), GPDMA state `0x2` (`BUSY`), no SPI/GPDMA error code, SPI `CR2=0x12`, GPDMA `CBR1=0x3f8`, source `0x20000046`, destination `0x46002020` (SPI3 secure TXDR alias). GPDMA starts but stalls almost immediately without a HAL error; next GPDMA variable to test is channel port allocation/request pacing rather than buffer placement. |
| 2026-05-29 | PH1-DISPLAY-GPDMA-52ROW-PORT1-PORT1 | `fw0` / GPDMA1 CH0 SPI3 TX + normal-RAM TX buffer | FAIL_OBSERVED | `firmware/peepshow_hw5_fw0/__fw0_gpdma_52row_port1_port1_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_gpdma_52row_port1_port1_flash_stdout.txt` | CubeMX regenerated GPDMA1 channel 0 with `TransferAllocatedPort = DMA_SRC_ALLOCATED_PORT1|DMA_DEST_ALLOCATED_PORT1`. Firmware still explicitly relinked `hspi3.hdmatx` to `handle_GPDMA1_Channel0`, used the normal-RAM TX staging buffer, and tested a 52-row Sharp row-window stream at `1000.0 KBits/s`. Bounded build passed with elapsed `1.085 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.175 s`, no leftover Programmer processes. User still observed a blank display. This rules out the simple GPDMA port0/port1 allocation variants tested so far; GPDMA SPI3 TX remains unvalidated and should not block display progress. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-FULLSCREEN-48ROW-CHUNKS | `fw0` / LPDMA1 CH0 SPI3 TX + SRAM4 TX buffer | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_fullscreen_48chunks_build_stdout.txt`; `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.map` | GPDMA was removed from `fw0`, leaving SPI3 TX linked only to `LPDMA1_REQUEST_SPI3_TX`. The display staging buffer was restored to `.sram4`, and firmware now presents a full-screen invert as four valid Sharp row-window DMA transactions: `48 + 48 + 48 + 24` rows. Bounded build passed with RAM `5560 B`, ROM `37456 B`, SRAM4 `3368 B`, elapsed `2.902 s`, exit code `0`, no leftover build processes. User confirmed visible full-screen inversion. This validates the current full-screen LPDMA display update strategy at CubeMX-calculated `1000.0 KBits/s`; agent-run flash artifact for this exact build was not captured because the final visual confirmation came after user-side flashing/running. |
| 2026-05-29 | PH1-DISPLAY-STATIC-HOLD | `fw0` / one-shot LPDMA full-screen pattern | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_static_hold_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_static_hold_flash_stdout.txt` | Firmware wrote a one-shot full-screen `0xAA` pattern using validated LPDMA chunks (`48 + 48 + 48 + 24`) and stopped issuing further display writes after the first successful flush. Bounded build passed with elapsed `1.301 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.347 s`, no leftover Programmer processes. User observed a static pattern of equally spaced pixels, confirming the panel holds written image data after display writes stop. RTC/EXTCOMIN configuration was unchanged from the previously measured 1 Hz path; a fresh PC13 probe was not taken during this specific static-hold test. |
| 2026-05-29 | PH1-DISPLAY-LPDMA-PARTIAL-BAND | `fw0` / LPDMA full-screen base pattern + partial row-window update | PASS | `firmware/peepshow_hw5_fw0/__fw0_lpdma_partial_band_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_lpdma_partial_band_flash_stdout.txt` | Firmware first wrote a one-shot full-screen `0xAA` base pattern using validated LPDMA chunks (`48 + 48 + 48 + 24`), then changed framebuffer rows `73-96` to `0x00` and flushed only that 24-row row-window using `LCD_FlushRows_DMA()`. Bounded build passed with elapsed `0.754 s`, exit code `0`, no leftover build processes. Bounded flash verify/reset passed with elapsed `0.880 s`, no timeout, no leftover Programmer processes. User observed a static full-screen pattern plus a static solid bar, confirming partial row-window update works and unmodified rows hold their previous image data. |
| 2026-05-29 | PH1-DISPLAY-RENDERER-DIRTY-ROWS | `fw0` / minimal renderer dirty-row present path | PASS | `firmware/peepshow_hw5_fw0/__fw0_display_renderer_dirty_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_display_renderer_dirty_flash_stdout.txt` | Added a minimal renderer layer that owns the framebuffer and dirty-row map while the `LS013B7DH05` driver remains the Sharp transport layer and enforces `LCD_DMA_MAX_ROWS_PER_TRANSFER=48`. Firmware proof used renderer fill/full-present for the base `0xAA` pattern, then marked rows `73-96` dirty and presented only those rows for the solid band. Bounded build passed with elapsed `1.100 s`, exit code `0`, no leftover build processes; stderr contained only the existing CMake dev warning from regeneration. Bounded flash verify/reset passed with elapsed `0.891 s`, no timeout, no leftover Programmer processes. User observed the same static pattern plus static solid bar, confirming the renderer dirty-row path preserves the validated display behavior. |
| 2026-05-29 | PH1-DISPLAY-SELFTEST-CLEANUP | `fw0` / renamed Phase 1 display self-test | PASS | `firmware/peepshow_hw5_fw0/__fw0_display_selftest_cleanup_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_display_selftest_cleanup_flash_stdout.txt` | Renamed the bring-up loop from invert-specific scaffolding to `PS_Phase1_DisplaySelfTestStep()` and renamed the probe to `g_ps_phase1_display_selftest_probe`; display behavior intentionally unchanged. Bounded build passed with elapsed `0.736 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.237 s`, no timeout, no leftover Programmer processes. User confirmed the display still showed the same static pattern plus solid bar. |
| 2026-05-29 | PH1-DISPLAY-UNIFORM-FILL-POLARITY | `fw0` / renderer full-screen `0x00` then `0xFF` fills | PASS | `firmware/peepshow_hw5_fw0/__fw0_display_uniform_fill_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_display_uniform_fill_flash_stdout.txt` | Firmware self-test presented full-screen `0x00` for about one second, then full-screen `0xFF` and stopped issuing further display writes. Bounded build passed with elapsed `0.745 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed with elapsed `0.836 s`, no timeout, no leftover Programmer processes. After power cycling to observe startup, user saw all black briefly followed by all white permanently. This confirms framebuffer byte value `0x00` renders black, `0xFF` renders white, and the panel holds the final white frame. |
| 2026-05-29 | PH2-STORAGE-OCTOSPI-JEDEC-STATUS | `fw0` / OCTOSPI1 polling command probe | PASS | `firmware/peepshow_hw5_fw0/__fw0_octospi_generated_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_jedec_probe_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_jedec_probe_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_jedec_probe_gdb_stdout_2.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_jedec_probe_gdbserver_stdout_2.txt` | User regenerated `fw0` with OCTOSPI1 quad pins and conservative PLL2Q clock. Generated `.ioc` has `PA0` NCS, `PB10` CLK, `PB1/PB0/PA7/PA6` IO0..IO3, `OCTOSPI1.DeviceSize=24`, `ClockPrescaler=8`, `ChipSelectHighTime=2`, and `OCTOSPIMFreq_Value=64000000`; no FileX, LevelX, USBX, ThreadX, or GPDMA storage path was enabled. Bounded generated build passed with elapsed `4.035 s`, exit code `0`, no leftover build processes; stderr contained the existing CMake dev warning plus HAL-driver warnings. Added a non-destructive polling probe that sends release-from-deep-power-down, reads JEDEC ID `0x9F`, and reads status/config commands `0x05/0x35/0x15`; probe build passed with elapsed `0.814 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.087 s`, no timeout, no leftover Programmer processes. Bounded non-resume GDB readback showed `ospi_kernel_hz=64000000`, `release_status=0`, `jedec_status=0`, `status1_status=0`, `status2_status=0`, `status3_status=0`, JEDEC ID `1F 42 18`, status bytes `00 00 00`, `ospi_error=0`, `complete=1`; no erase/program/DMA/filesystem operation was attempted. |
| 2026-05-29 | PH2-STORAGE-OCTOSPI-SCRATCH-RW | `fw0` / OCTOSPI1 polling erase/program/readback probe | PASS | `firmware/peepshow_hw5_fw0/__fw0_octospi_scratch_rw_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_scratch_rw_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_scratch_rw_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_scratch_rw_gdbserver_stdout.txt` | User approved using the final sector for immediate destructive bring-up tests. Scratch range recorded as `0x00FFF000..0x00FFFFFF`; firmware test touched the first `256` bytes. Sequence: write-enable, 4 KiB sector erase, busy-poll, read blank, write-enable, page program deterministic pattern `0xA5 ^ index`, busy-poll, polling readback compare, write-enable, cleanup sector erase, busy-poll, final blank readback. Bounded build passed with elapsed `0.864 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.356 s`, no timeout, no leftover Programmer processes. Bounded non-resume GDB readback showed every command/wait/read status `0`, JEDEC ID still `1F 42 18`, `scratch_address=0x00FFF000`, `test_length=256`, `erase_blank_mismatch_count=0`, `verify_mismatch_count=0`, `cleanup_blank_mismatch_count=0`, first 16 verified bytes `A5 A4 A7 A6 A1 A0 A3 A2 AD AC AF AE A9 A8 AB AA`, `ospi_error=0`, `complete=1`. The test left the scratch sector erased at completion. |
| 2026-05-29 | PH2-STORAGE-OCTOSPI-DPD-WAKE | `fw0` / OCTOSPI1 polling deep-power-down and release probe | PASS | `firmware/peepshow_hw5_fw0/__fw0_octospi_dpd_wake_build_stdout_2.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dpd_wake_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dpd_wake_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dpd_wake_gdbserver_stdout.txt` | Extended the polling scratch probe to enter deep power-down with command `0xB9`, delay, release with command `0xAB`, delay, re-read JEDEC ID, and re-read the erased scratch bytes. Corrected bounded build passed with elapsed `0.709 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed with elapsed `1.020 s`, no timeout, no leftover Programmer processes. Bounded non-resume GDB readback showed `deep_power_down_status=0`, `wake_status=0`, `wake_jedec_status=0`, `wake_read_status=0`, wake JEDEC ID `1F 42 18`, `wake_blank_mismatch_count=0`, `ospi_error=0`, `complete=1`, and no leftover GDB/ST-Link processes. The scratch sector remained erased after the wake revalidation. |
| 2026-05-29 | PH2-STORAGE-OCTOSPI-GPDMA-READ-FIRST-PROBE | `fw0` / OCTOSPI1 GPDMA read of programmed scratch pattern | PASS | `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_read_probe_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_read_probe_flash_retry_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_read_probe_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_irq_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_irq_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_irq_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_irq_gdbserver_stdout.txt` | User regenerated `fw0` with GPDMA OCTOSPI channels. Final generated MSP configured `GPDMA1_Channel5` as peripheral-to-memory with fixed source/incrementing destination and `GPDMA1_Channel4` as memory-to-peripheral with incrementing source/fixed destination. First flash attempt failed because CubeProgrammer saw target voltage `0.00 V`; retry saw `1.81 V` and verify/reset passed. Initial DMA read moved correct data but timed out waiting for the HAL OSPI completion callback because GPDMA completion enables OCTOSPI transfer-complete interrupt and no OCTOSPI1 IRQ handler/NVIC enable existed yet. Added `OCTOSPI1_IRQHandler()` and OCTOSPI1 NVIC enable. Corrected build passed with elapsed `0.740 s`, exit code `0`, empty stderr, and no leftover build processes. Bounded flash verify/reset passed at target voltage `1.80 V` with elapsed `1.004 s` and no leftover Programmer processes. Bounded non-resume GDB readback showed `dma_read_status=0`, `dma_wait_status=0`, `dma_verify_mismatch_count=0`, first 16 DMA-read bytes `A5 A4 A7 A6 A1 A0 A3 A2 AD AC AF AE A9 A8 AB AA`, `ospi_error=0`, `complete=1`, and no leftover GDB/ST-Link processes. Polling verify, cleanup erase, DPD wake, and final blank readback also passed. |
| 2026-05-29 | PH2-STORAGE-OCTOSPI-GPDMA-PROGRAM-READBACK | `fw0` / OCTOSPI1 GPDMA page program and GPDMA readback | PASS | `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_program_probe_build_3_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_program_probe_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_program_probe_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_octospi_dma_program_probe_gdbserver_stdout.txt` | Extended the Phase 2 scratch probe to program the 256-byte scratch page at `0x00FFF000` via `HAL_OSPI_Transmit_DMA()` on `GPDMA1_Channel4`, then verify using the validated `HAL_OSPI_Receive_DMA()` read path on `GPDMA1_Channel5` and a secondary polling read. Bounded build passed with elapsed `0.770 s`, exit code `0`, empty stderr, no compiler warnings in the retained final build artifact, and no leftover build processes. Bounded flash verify/reset passed at target voltage `1.79 V` with elapsed `1.681 s` and no leftover Programmer processes. Bounded non-resume GDB readback showed `dma_program_status=0`, `dma_program_wait_status=0`, `program_wait_status=0`, `dma_read_status=0`, `dma_wait_status=0`, `dma_verify_mismatch_count=0`, `verify_mismatch_count=0`, first 16 bytes `A5 A4 A7 A6 A1 A0 A3 A2 AD AC AF AE A9 A8 AB AA`, `cleanup_blank_mismatch_count=0`, `wake_blank_mismatch_count=0`, `ospi_error=0`, and `complete=1`; no leftover GDB/ST-Link processes. |
| 2026-05-29 | PH3-AUDIO-BBB-FIXED-TONE | `fw0` / LPTIM1_CH1 fixed BBB tone | PASS | `firmware/peepshow_hw5_fw0/__fw0_audio_phase3a_generated_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_tone_irqfix_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_tone_irqfix_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_tone_irqfix_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_tone_irqfix_gdbserver_stdout.txt` | User regenerated `fw0` with `PC9` `SD_MODE` GPIO default low and `PB2` `BUZZ` on `LPTIM1_CH1`, with LPTIM1 clocked from MSIK at `4 MHz`. Initial generated build passed. Added a one-shot Phase 3 BBB probe that forced `SD_MODE` low, reconfigured LPTIM1 for a `2 kHz` active-high PWM tone (`period=1999`, `pulse=1000`) for about `500 ms`, then stopped PWM and kept `SD_MODE` low. CubeMX regeneration had removed the required `OCTOSPI1_IRQHandler()` while preserving the OCTOSPI1 NVIC enable, causing the first run to enter the default handler during the existing Phase 2 storage probe; restoring the IRQ handler fixed the regression. Corrected build passed with elapsed `0.747 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed at target voltage `1.79 V` with elapsed `1.366 s` and no leftover Programmer processes. Attach-mode bounded GDB readback showed `lptim_kernel_hz=4000000`, `requested_hz=2000`, `period=1999`, `pulse=1000`, `reinit_status=0`, `channel_config_status=0`, `start_status=0`, `stop_status=0`, `sd_mode_state_before=0`, `sd_mode_state_after=0`, `complete=1`; existing Phase 2 storage probe still completed successfully. User reported hearing a beep. |
| 2026-05-29 | PH3-AUDIO-BBB-SWEEP | `fw0` / LPTIM1_CH1 rising BBB sweep | PASS | `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_sweep_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_sweep_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_sweep_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_sweep_gdbserver_stdout.txt` | Replaced the fixed-tone BBB proof with a bounded rising sweep on `PB2` / `LPTIM1_CH1`, with `SD_MODE` forced low before and after so the speaker amp path stayed disabled. Sweep used MSIK `4 MHz`, `800 Hz` start, `4000 Hz` end, `24` steps, and `35 ms` per step. Bounded build passed with elapsed `0.902 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed at target voltage `1.79 V` with elapsed `1.128 s` and no leftover Programmer processes. Attach-mode bounded GDB readback showed `lptim_kernel_hz=4000000`, `sweep_start_hz=800`, `sweep_end_hz=4000`, `sweep_steps_requested=24`, `sweep_steps_completed=24`, `failed_step=0`, final `period=999`, final `pulse=500`, `start_status=0`, `stop_status=0`, `final_step_status=0`, `sd_mode_state_before=0`, `sd_mode_state_after=0`, `complete=1`; existing Phase 2 storage probe still completed successfully. User reported hearing the sweep. |
| 2026-05-29 | PH3-AUDIO-BBB-MELODY-GAP-PATTERN | `fw0` / LPTIM1_CH1 short BBB pattern with gaps | PASS_WITH_NOTE | `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_pattern_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_pattern_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_pattern_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_bbb_pattern_gdbserver_stdout.txt` | Replaced the sweep proof with a six-step BBB melody/gap pattern on `PB2` / `LPTIM1_CH1`, with explicit PWM stop and gap between notes and `SD_MODE` held low before and after. Pattern frequencies were `1047`, `1319`, `1568`, `2093`, `1568`, and `2093 Hz`, with `90 ms` tone and `70 ms` gap per step. Bounded build passed with elapsed `0.809 s`, exit code `0`, empty stderr, no leftover build processes. Bounded flash verify/reset passed at target voltage `1.81 V` with elapsed `1.103 s` and no leftover Programmer processes. Attach-mode bounded GDB readback showed `pattern_steps_requested=6`, `pattern_steps_completed=6`, `failed_step=0`, `start_status=0`, `stop_status=0`, `final_step_status=0`, final `period=1910`, final `pulse=955`, `sd_mode_state_before=0`, `sd_mode_state_after=0`, `complete=1`; existing Phase 2 storage probe still completed successfully. User heard the melody but perceived it closer to five tones; treat this as auditory pass with note that tone/gap timing or piezo response may mask one step. |
| 2026-05-29 | PH3-AUDIO-SPEAKER-SAI-DMA-TONE | `fw0` / SAI1_A TX + GPDMA1 CH3 + MAX98357A `SD_MODE` | PASS | `firmware/peepshow_hw5_fw0/__fw0_audio_speaker_sai_dma_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_speaker_sai_dma_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_speaker_sai_dma_gdb_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_speaker_sai_dma_drive_settle_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_speaker_sai_dma_drive_settle_flash_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_audio_speaker_ppk2_ladder_build_stdout.txt` | User regenerated `fw0` with SAI1_A on `PA8` SCK, `PB9` FS, `PA10` SD, `PC9` `SD_MODE`, `PLL2P = 4.096 MHz`, and GPDMA1 CH3 for SAI1_A TX. User manually corrected CubeMX-generated SAI DMA linkage from RX to TX and enabled/restored OCTOSPI1 NVIC/IRQ after regeneration. Added a bounded speaker probe that generated a `1 kHz`, 16 kHz-sample-rate PCM buffer, started `HAL_SAI_Transmit_DMA()`, enabled `SD_MODE`, delayed, stopped DMA, and returned `SD_MODE` low. First bounded build passed with elapsed `0.834 s`, exit code `0`, empty stderr, and no leftover build processes. First bounded flash verified/reset at `1.80 V` with elapsed `1.523 s` and no leftover Programmer processes, but user heard nothing. Attach-mode GDB readback showed the firmware path did run: `sai_kernel_hz=4096000`, `speaker_sample_rate_hz=16000`, `speaker_tone_hz=1000`, `speaker_start_status=0`, `speaker_stop_status=0`, `speaker_half_cplt_count=50`, `speaker_cplt_count=49`, `speaker_error_count=0`, `sai_state_after=1`, `sai_error_after=0`, `sd_mode_state_after=0`, and `complete=1`. A second probe enabled SAI output drive, asserted `SD_MODE` before DMA with a `20 ms` settle, and increased proof-tone duration/amplitude; bounded build passed with elapsed `0.804 s`, exit code `0`, empty stderr, no leftover build processes, and bounded flash verified/reset at `1.79 V` with elapsed `1.163 s` and no leftover Programmer processes. User then found the speaker connection was loose, replugged/rebooted, and heard a loud tone. A follow-up PPK2 ladder test used amp-on silence, three sine amplitudes, and one short square comparison; bounded build passed with elapsed `0.780 s`, exit code `0`, empty stderr, and no leftover build processes. User measured first three segments at about `10 mA` and too soft, sine amplitude `3000` at about `15 mA` and subjectively good, and short square amplitude `1000` at about `10 mA` and also acceptable. Treat these as bring-up cue data only; final volume/current policy remains pending mixer and asset validation. |
| 2026-05-30 | PH4-BUTTONS-RAW-EXTI | `fw0` / PA4 Start + PB5..PB8 A/B/L/R EXTI | PASS | `firmware/peepshow_hw5_fw0/__fw0_phase4_buttons_probe_build_stdout.txt`; `firmware/peepshow_hw5_fw0/__fw0_phase4_buttons_u5_exti_callbacks_build_stdout.txt` | User regenerated `fw0` with `BTN_START` on `PA4` as rising/falling EXTI with pull-up, and `BTN_A/B/L/R` on `PB5/PB6/PB7/PB8` as rising/falling EXTI with pull-downs. Initial raw probe used the legacy `HAL_GPIO_EXTI_Callback()` name and showed idle polarity was correct but no edge counts, because STM32U5 HAL dispatches to separate rising/falling callbacks. Firmware was corrected to implement `HAL_GPIO_EXTI_Rising_Callback()` and `HAL_GPIO_EXTI_Falling_Callback()`. Bounded build passed with elapsed `1.163 s`, exit code `0`, empty stderr, and no leftover build processes. User-side flash/debug readback after button presses showed `idle_mask=0x0`, `current_mask=0x0`, `total_edges=0x18`, Start `3` presses / `3` releases, A `2` / `2`, B `3` / `3`, L `2` / `2`, and R `2` / `2`. This validates normal button GPIO polarity and EXTI routing. BOOT0, debounce timing, long press, repeat, chord, Start shipping-prep, and wake behavior remain pending. |
| 2026-05-30 | PH4-LIGHT-POLLED-ADC | `fw0` / `PC2` `PHOT_EN` + `PC3` `ADC1_IN4` polled ADC | PASS_WITH_LIMITATION | `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.elf` | User regenerated `fw0` with `PC2` as regular GPIO output default low and `PC3` as `ADC1_IN4` regular conversion with `ADC_SAMPLETIME_814CYCLES`. Added a minimal polled probe `g_ps_phase5_light_probe` that calibrates ADC1, pulses `PHOT_EN` high around each sample, samples every `250 ms`, and leaves `PHOT_EN` low after each sample. Bounded build passed with elapsed `5570 ms`, exit code `0`, and no leftover build processes. User-side flash/debug readback showed direct light down to `0x0011`, ambient samples around `0x38AF..0x3995`, covered samples around `0x3F9F..0x3FAB`, `adc_error_count=0`, calibration/start/poll/stop statuses all `0`, and `phot_en_state_after=0`. This validates the raw ADC/sensor-enable path and confirms inverse ADC polarity for the mounted circuit: brighter light gives lower ADC counts. Settle time, final calibration bands, normalized mapping, streaming policy, and power/sleep behavior remain pending. |
| 2026-05-30 | PH4-IMU-LIS2DUX12-IDENTITY | `fw0` / I2C3 + ST LIS2DUX12 driver read-only probe | PASS | `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.elf` | User added the official ST LIS2DUX12 driver to `fw0`; `Core/Src/LIS2DUX12.c` was added to the build. Added a read-only `g_ps_phase4_imu_probe` with the driver write hook stubbed to fail, so the probe cannot configure or reset the IMU. Bounded build passed with elapsed `927 ms`, exit code `0`, and no leftover build processes. User-side flash/debug readback showed expected address `0x18` ACKed with shifted HAL address `0x30`, alternate address `0x19` NACKed with HAL error `0x4`, driver and raw `WHO_AM_I` reads both returned `0x47`, raw `STATUS=0x02`, `CTRL1=0x10`, `CTRL4=0x00`, `MD1_CFG=0x00`, `i2c_error_after=0`, and `complete=1`. This validates the I2C route and LIS2DUX12 identity only. Step counter, embedded functions, INT1 on `PB14`, motion/tap/shake/tilt events, low-power retention, and fault recovery remain pending. |
| 2026-05-30 | PH4-TMAG3001-RAW-I2C-SCAN | `fw0` / I2C3 read-only address scan | BLOCKED_HW | `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.elf` | Added a read-only TMAG3001 preliminary probe that scans expected strap addresses `0x34..0x37` and then performs a full ACK-only I2C scan from `0x08..0x77`. User-side readback showed `0x34..0x37` all NACKed with HAL error `0x4`, `detected_public_addr_7bit=0`, and full scan found only two devices: bitmap entries decode to `0x18` and `0x46`, matching the already-validated LIS2DUX12 and ADP5360. This confirms I2C3 is operational but TMAG3001 is not visible on the bus. Next action is hardware inspection: TMAG VCC/GND, SDA/SCL continuity to I2C3, ADDR strap, assembly orientation, and any unpowered/held state. No TMAG register reads or writes succeeded. |
| 2026-05-30 | PH7-NINA-UART-AT-PROOF | `fw0` / LPUART1 RTS/CTS + NINA reset GPIO | PASS_WITH_LIMITATION | `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.elf` | User regenerated `fw0` with NINA pins and `LPUART1` RTS/CTS, then corrected CubeMX baud from `9600` to `115200`. Added a bounded `g_ps_phase7_nina_probe` that keeps `NINA_SW1`, `NINA_SW2`, `NINA_DTR`, and `NINA_DSR` high-Z/no-pull, asserts `NINA_NRST` low, releases it high, captures boot UART bytes, sends read-only `AT`/`ATI` probes, and reasserts reset low at completion. User-side readback showed `PC6` ODR set high after release, `nrst_after_release=0` immediately, then `nrst_after_boot_wait=1` after the boot delay, so reset release is delayed but succeeds. Clean `115200` run captured boot banner `00 0d 0a 2b 53 54 41 52 54 55 50 0d 0a` (`+STARTUP`), `AT` returned echo plus `OK`: `41 54 0d 0a 0d 0a 4f 4b 0d 0a`, `ATI` returned echo plus `ERROR`, UART state/error clean, and `nrst_after_final_reset=0`. This validates preliminary NINA reset release, boot banner, LPUART TX/RX, and hardware flow-control path only. The correct version/profile query command remains pending. Advertising, connection, pairing/bonding, payload limits, sleep/current, and comm-owner integration remain pending. |
| 2026-05-30 | PH7-NINA-IDENTITY-DISCOVERY | `fw0` / bounded u-connectXpress identity command scan | PASS | `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.elf`; official u-blox `u-connectXpress AT commands manual` `UBX-14044127` | Extended the NINA probe with read-only general identity/version commands: `AT`, `ATI0`, `ATI9`, `ATI10`, `AT+CGMI`, `AT+CGMM`, `AT+CGMR`, `AT+CGSN`, `AT+GMI`, `AT+GMM`, `AT+GMR`, and `AT+GSN`; no advertising, pairing, bonding, saved config, or BLE state change commands were sent. User-side readback showed all 12 command transmissions returned HAL status `0` and all command responses ended in `OK`. Captured identity: module `NINA-B112-04B-00` from `ATI0`; software/version string `5.0.0-011` and build/profile string `NINA-B11X-5.0.0-011-0-g1aaa693` from `ATI9`; `ATI10` returned `B5BC4AA6C339F8EA`; manufacturer `u-blox` from `+CGMI`/`+GMI`; model family `NINA-B1` from `+CGMM`/`+GMM`; firmware `5.0.0-011` from `+CGMR`/`+GMR`; serial `0141431523137804` from `+CGSN`/`+GSN`. Probe again reasserted `NINA_NRST` low at completion. This validates bounded NINA command mode and identity discovery only; BLE advertising, connections, pairing, bonding persistence, payload limits, sleep/current, and comm-owner integration remain pending. |
| 2026-05-30 | PH7-NINA-SPS-PHONE-TX | `fw0` / Android Serial Bluetooth Terminal + NINA SPS | PASS_WITH_LIMITATION | `firmware/peepshow_hw5_fw0/build/Debug/peepshow_hw5_fw0.elf`; official u-blox `u-connectXpress AT commands manual` `UBX-14044127` | User connected using Android app `Serial Bluetooth Terminal` and reported seeing `peepshow nina sps hello` from the firmware, then later saw the longer prompt `peepshow nina sps hello` / `type text and press send`. Probe readback showed `phone_detected=1`, `phone_sps_detected=1`, `data_mode_tx_status=0`, `hello_tx_status=0`, and captured `+UUBTACLC` plus `+UUDPC` SPS data-peer events. This validates phone-to-NINA BLE connection, NINA SPS peer open, STM32 UART TX to NINA, and NINA SPS TX to phone. User typed `hello nina`, but repeated readbacks still showed `phone_data_rx_len=0` and `phone_echo_tx_count=0`, so phone-to-board payload receive/echo remains pending. The module firmware is `5.0.0-011`; `+UDSF` bit 1 data-in-AT-mode is documented by u-blox as NINA-B1 SW7.0.0 onward, so this HW5 module must use transparent data mode or an app that writes the correct SPS RX characteristic for phone-to-board payload testing. |

---

## Temporary Measures Register

| ID | Introduced | Scope | Exit Criteria | Owner | Status |
|---|---|---|---|---|---|
| TMP-XXX | YYYY-MM-DD | TBD | TBD | TBD | active |

---

## Open Issues Blocking Completion

| ID | Blocking Phase | Summary | Owner | Next Action |
|---|---|---|---|---|
| BUG-XXX | Phase X | TBD | TBD | TBD |
